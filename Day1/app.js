const express=require("express")

const app=express() //server created 


app.listen(3000) //server start karta hai








//package.json-keeps the dependencies my code has on other packages

//package-lock.json maintains a dependencies on dependencies for all the packages my dependencies depend upon
//node_modules containes all the code of the dependencies that is needed to run my code in their specific folders